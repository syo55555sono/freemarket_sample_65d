.telephonepage
  =render "signup/header"
  
  .main
    %section.main-content
      %h2.main-content__header
        電話番号の確認
      = form_with model: @user, local: true, url: step4_signup_index_path, method: 'GET' do |f|
        .form__content
          .form__content__group
            %label  携帯電話の番号
            = f.text_field :phone_tel, autofocus: true, autocomplete: "phone_tel", placeholder:'携帯電話の番号を入力', class: "phone-number"
          %p 本人確認のため、携帯電話のSMS(ショートメッセージサービス)を利用して認証を行います。

          = f.submit 'SMSを送信する', class:"form__content__submit"
              
          .form__content__attention
            ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。
            .form__content__attention__user-information
              = link_to '電話番号の確認が必要な理由 〉', '#'

  =render "signup/footer"